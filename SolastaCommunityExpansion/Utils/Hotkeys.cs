#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace SolastaCommunityExpansion.Utils;
#pragma warning restore IDE0130 // Namespace does not match folder structure

internal static class Hotkeys
{
    // Toggle HUD components
    internal const InputCommands.Id CTRL_SHIFT_C = (InputCommands.Id)44440000;
    internal const InputCommands.Id CTRL_SHIFT_L = (InputCommands.Id)44440001;
    internal const InputCommands.Id CTRL_SHIFT_M = (InputCommands.Id)44440002;
    internal const InputCommands.Id CTRL_SHIFT_P = (InputCommands.Id)44440003;
    internal const InputCommands.Id CTRL_SHIFT_H = (InputCommands.Id)44440004;

    // Debug Overlay
    internal const InputCommands.Id CTRL_SHIFT_D = (InputCommands.Id)44440005;

    // Export Character
    internal const InputCommands.Id CTRL_SHIFT_E = (InputCommands.Id)44440006;

    // Spawn Encounter
    internal const InputCommands.Id CTRL_SHIFT_S = (InputCommands.Id)44440007;

    // Teleport Party
    internal const InputCommands.Id CTRL_SHIFT_T = (InputCommands.Id)44440008;

    // Zoom Camera
    internal const InputCommands.Id CTRL_SHIFT_Z = (InputCommands.Id)44440009;
}
