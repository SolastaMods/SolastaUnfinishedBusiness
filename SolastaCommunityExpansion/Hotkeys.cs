#pragma warning disable IDE0130 // Namespace does not match folder structure
namespace SolastaCommunityExpansion
#pragma warning restore IDE0130 // Namespace does not match folder structure
{
    internal class Hotkeys
    {
        // Hide / Show HUD components
        internal const InputCommands.Id CTRL_C = (InputCommands.Id)44440000;
        internal const InputCommands.Id CTRL_L = (InputCommands.Id)44440001;
        internal const InputCommands.Id CTRL_M = (InputCommands.Id)44440002;
        internal const InputCommands.Id CTRL_P = (InputCommands.Id)44440003;
        internal const InputCommands.Id CTRL_H = (InputCommands.Id)44440004;

        // Export Hero
        internal const InputCommands.Id CTRL_E = (InputCommands.Id)44440005;

        // Spawn Encounter
        internal const InputCommands.Id CTRL_SHIFT_E = (InputCommands.Id)44440006;

        // Teleport Party
        internal const InputCommands.Id CTRL_SHIFT_T = (InputCommands.Id)44440007;

        // Debug Overlay
        internal const InputCommands.Id CTRL_SHIFT_D = (InputCommands.Id)44440008;
    }
}
